{
    "name": "DevEnviron OpenWeights",
    "image": "mcr.microsoft.com/devcontainers/python:3.12-bookworm",
    "features": {
        // Installs a modern Node.js version (latest LTS or specific)
        "ghcr.io/devcontainers/features/node:1": {
            "version": "lts"
        }
    },
    "postCreateCommand": "sudo apt-get update && sudo apt-get install -y vim curl &&  pip install ddgs && curl -fsSL https://pi.dev/install.sh | sh && pi install npm:pi-ollama-cloud && pi install npm:pi-search-hub && pi install npm:pi-footer",
}
